name: Deploy to Dev

# Manual only — never triggers on push, so a bad dev build can never touch prod.
on:
  workflow_dispatch:

jobs:
  deploy:
    name: SSH Deploy (dev)
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEV_DEPLOY_HOST }}
          username: ${{ secrets.DEV_DEPLOY_USER }}
          key: ${{ secrets.DEV_DEPLOY_SSH_KEY }}
          script: |
            set -e

            REPO_URL="https://github.com/HammerOfSteel/music_visualizer_dancing_salamanders.git"
            DEPLOY_DIR="/opt/music-visualizer-dev"
            DOMAIN="musicdev.dancingsalamanders.com"
            NGINX_SITE="/etc/nginx/sites-available/${DOMAIN}"
            NGINX_ENABLED="/etc/nginx/sites-enabled/${DOMAIN}"

            # ── 1. Clone or pull latest code ────────────────────────────
            if [ ! -d "${DEPLOY_DIR}/.git" ]; then
              sudo git clone "${REPO_URL}" "${DEPLOY_DIR}"
            else
              cd "${DEPLOY_DIR}"
              sudo git fetch origin main
              sudo git reset --hard origin/main
            fi

            cd "${DEPLOY_DIR}"

            # ── 2. Build and start production container ──────────────────
            export WEB_PORT="3009"
            sudo -E docker compose -f docker-compose.prod.yml up --build -d

            # ── 3. Install nginx site config (first deploy only) ─────────
            if [ ! -f "${NGINX_SITE}" ]; then
              sudo cp "${DEPLOY_DIR}/deploy/nginx-music-dev.conf" "${NGINX_SITE}"
              sudo ln -sf "${NGINX_SITE}" "${NGINX_ENABLED}"
              sudo nginx -t
              sudo systemctl reload nginx
              echo "Nginx site installed."
            fi

            # ── 4. Obtain TLS certificate (once per domain) ───────────────
            if [ ! -f "/etc/letsencrypt/live/${DOMAIN}/fullchain.pem" ]; then
              sudo certbot --nginx -d "${DOMAIN}" --non-interactive --agree-tos --email "info@dancingsalamanders.com"
              echo "Certificate obtained for ${DOMAIN}."
            fi

            # ── 5. Remove dangling images ──────────────────────────────────
            sudo docker image prune -f

            echo "Deployment complete: https://${DOMAIN}"
